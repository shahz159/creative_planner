import { TaskDateAgoPipe } from './task-date-ago.pipe';

describe('TaskDateAgoPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskDateAgoPipe();
    expect(pipe).toBeTruthy();
  });
});
