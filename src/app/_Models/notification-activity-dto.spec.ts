import { NotificationActivityDTO } from './notification-activity-dto';

describe('NotificationActivityDTO', () => {
  it('should create an instance', () => {
    expect(new NotificationActivityDTO()).toBeTruthy();
  });
});
