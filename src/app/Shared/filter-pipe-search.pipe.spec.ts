import { FilterPipeSearchPipe } from './filter-pipe-search.pipe';

describe('FilterPipeSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipeSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
