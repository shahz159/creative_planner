import { FocusOnClickDirective } from './focus-on-click.directive';

describe('FocusOnClickDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusOnClickDirective();
    expect(directive).toBeTruthy();
  });
});
