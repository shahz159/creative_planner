import { GrdFilterPipePipe } from './grd-filter-pipe.pipe';

describe('GrdFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new GrdFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
