import { ProjectDetailsDTO } from './project-details-dto';

describe('ProjectDetailsDTO', () => {
  it('should create an instance', () => {
    expect(new ProjectDetailsDTO()).toBeTruthy();
  });
});
