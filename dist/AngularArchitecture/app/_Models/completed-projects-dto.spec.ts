import { CompletedProjectsDTO } from './completed-projects-dto';

describe('CompletedProjectsDTO', () => {
  it('should create an instance', () => {
    expect(new CompletedProjectsDTO()).toBeTruthy();
  });
});
