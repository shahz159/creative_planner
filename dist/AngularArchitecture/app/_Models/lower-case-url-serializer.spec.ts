import { LowerCaseUrlSerializer } from './lower-case-url-serializer';

describe('LowerCaseUrlSerializer', () => {
  it('should create an instance', () => {
    expect(new LowerCaseUrlSerializer()).toBeTruthy();
  });
});
